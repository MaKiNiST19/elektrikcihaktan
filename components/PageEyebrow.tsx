export function PageEyebrow({ className = "" }: { className?: string }) {
  return (
    <p className={`text-shine font-semibold mb-3 uppercase tracking-wider text-sm ${className}`}>
      20+ Yıl Tecrübe · İzmir'in Tamamı
    </p>
  );
}
